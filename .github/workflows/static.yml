# ПРОСТОЙ ШАГ: Создаём базовую главную страницу
- name: Create simple index.html
  run: |
    cd src/site/notes/
    
    # Начинаем HTML
    echo '<!DOCTYPE html><html><head><meta charset="UTF-8"><title>Сады</title>' > index.html
    echo '<style>body{font-family:sans-serif; margin:40px;}</style></head>' >> index.html
    echo '<body><h1>🌿 Цифровые сады</h1><ul>' >> index.html
    
    # Добавляем ссылки на все папки
    for dir in */; do
      dir_name="${dir%/}"
      dir_url="${dir// /%20}"
      echo "<li><a href=\"$dir_url/\">📁 $dir_name</a></li>" >> index.html
    done
    
    # Завершаем
    garden_count=$(ls -d */ | wc -l)
    echo "</ul><p>Всего садов: <strong>$garden_count</strong></p>" >> index.html
    echo "<p><small>$(date)</small></p></body></html>" >> index.html
    
    echo "✅ Создан простой index.html с $garden_count садами"
